#include<bits/stdc++.h>
using namespace std;

struct Node
{
    int data;
    Node* left,*right; 
}

Node* getNode(int data)
{
    Node *temp = new Node;
    temp->left=NULL;
    temp->right=NULL;
}

Node* inOrder(int arr[], int l, )


int main()
{
    int n;
    cin>>n;
    int a[n];
    for(int i=0;i<n;i++)
    {
        cin>>a[i];
    }

}