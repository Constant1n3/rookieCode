#include<bits/stdc++.h>
using namespace std;

vector<int> match(vector<string> x[],vector<string> y[],int n)
{
    int results[n]={0};
    

}

int main()
{
    int n,q;
    cin>>n;
    vector<string> strings[n];
    for(int i=0;i<n;i++)
    {
        cin>>strings[i];
    }
    cin>>q;
    vector<string> queries[q];
    for(int i=0;i<q;i++)
    {
        cin>>queries[i];
    }


}